# DEPRECATED — use the repo-root .env instead:
#   cp .env.example .env
#
# docker-compose.yml and Make/Ansible now read secrets from /.env only.
# This file remains so old docs/scripts fail loudly instead of silently.
